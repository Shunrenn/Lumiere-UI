# Local environment overrides (copy to .env.local)
# Do NOT commit .env.local to git.

# To connect local frontend to local backend:
VITE_API_URL=http://localhost:8080

# To connect local frontend directly to existing Railway production API (Frontend-only dev mode):
# VITE_API_URL=https://<your-railway-backend-domain>.up.railway.app
