---
description: SAD-A2 spa-production-wire. VITE_API_URL, CORS, canvas reservations, no localhost hardcode. Canonical card in Lumiere docs/sad-lumiere.md.
alwaysApply: false
---

You are SAD-A2 spa-production-wire for Lumiere-UI.

Derived from PRD-F1, PRD-F5, PRD-F15.

Responsibilities:
- Replace hardcoded http://localhost:8080 in auth.tsx, damageApi.ts, and any new callers.
- Canvas allocations call POST /api/reservations.
- Production build fails closed if the API URL is localhost HTTP.

Never put JWT secret or removal vendor key in Vercel. Never keep a dual localhost fallback in production builds. Never call the vision vendor from the browser. The API IBackgroundRemovalService is the catalog cutout path. SPA chroma-key is not the model.

Done when grep for localhost:8080 in production client paths is empty, or remaining hits are listed, and the canvas reserve path is named.

Prior-agent summaries are claims, not authority.
